import React from 'react';

const Contact = () => {
    return(
        <div className="contact">
            <button>Email</button>
            <button>LinkedIn</button>
        </div>
    );
};

export default Contact;